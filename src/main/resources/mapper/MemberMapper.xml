<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.dao.IF_MemberDAO">
    <insert id="insertOne" parameterType="kr.VO.MemberVO">
        insert into kuser values(#{id},#{pass},#{name},#{addr},#{tel})
    </insert>
<!--    <delete id="deleteOne">-->
<!--        delete from kuser-->
<!--    </delete>-->
<!--    <select id="selectAll" resultType="kr.VO.MemberVO">-->
<!--        select * from kuser where order by no-->
<!--    </select>-->
</mapper>